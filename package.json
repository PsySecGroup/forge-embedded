{
  "name": "@psysecgroup/forge-embedded",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx serve .",
    "lint": "npx tsc --noEmit && npx eslint src/index.ts",
    "test": "npm run debug && npx uvu -r esbuild-register tests/",
    "debug": "asc src/index.ts --target debug",
    "build": "asc src/index.ts --target release",
    "compile": "npm run lint && npm run test && npm run debug && npm run build",
    "start": "npx serve ."
  },
  "author": "PsySecGroup",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/node": "^20.14.1",
    "assemblyscript": "^0.27.27",
    "esbuild": "^0.21.4",
    "esbuild-register": "^3.5.0",
    "eslint": "9.x",
    "globals": "^15.3.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.12.0",
    "uvu": "^0.5.6"
  },
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/release.js",
      "types": "./dist/release.d.ts"
    }
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.27.29"
  }
}
